services:
  # 1. لوحة التحكم (Frontend - Next.js)
  - type: web
    name: kurotek-admin
    env: node
    region: frankfurt
    rootDir: .
    buildCommand: npm install -g pnpm && pnpm install && pnpm run build
    startCommand: pnpm run start
    envVars:
      - key: NEXT_PUBLIC_API_URL
        fromService:
          type: web
          name: kurotek-backend
          envVar: RENDER_EXTERNAL_URL

  # 2. السيرفر والباك إند (Backend - NestJS)
  - type: web
    name: kurotek-backend
    env: node
    region: frankfurt
    rootDir: kurotek-backend
    buildCommand: npm install && npx prisma generate && npm run build
    startCommand: npx prisma migrate deploy && npm run start:prod
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: kurotek-postgres
          property: connectionString
      - key: JWT_SECRET
        generateValue: true

databases:
  # 3. قاعدة البيانات (PostgreSQL)
  - name: kurotek-postgres
    region: frankfurt
    databaseName: kurotekdb
    user: kurotek_admin
